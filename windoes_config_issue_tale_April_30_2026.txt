project builds using CMake: Build, and runs (both executibles)
HOWEVER my intellisense is throwing all kinds of problems
mainly OpenCV cascading errors (can't find the include)

cmake tools -- odd cmake location, command palatte for building
c/c++ tools -- uses its own thing for intellisense

AI ... actually located most of this

---------------- Fixing ---------------- 

Utilize the CMake that is bundled with a Visual Studio Installation

Visual Studio Build Tools amd64 (x64) 
MSVC
    Microsoft Visual C++ (MSVC) is a proprietary compiler toolset by Microsoft for C, C++, and C++/CLI programming languages

    C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Tools\MSVC\14.44.35207
    cl.exe and link.exe in \bin

    C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin
    cmake.exe

    these are all on my PATH 

---------------------


OpenCV is even on the includePath for cppTools

AND on my system PATH --- apparantly this is for EXECUTIBLES ONLY, NOT INCLUDE FILES. (cmake, cl and link .exes)

------ FIX 1 -------

so it TURNS OUT
    c_cpp tools not supported on cursor

use clangd extension -- "teach your Editor C++"

    code completion
    compile errors and warnings
    go-to-definition and cross references
    hover information and inlay hints
    include management
    code formatting
    simple refactorings

    https://clangd.llvm.org/installation#project-setup

    compile_commands.json -- provides compile commands for every source file in the project, 
        usually generated by build tools (not by mine :P)

    make file manually, how to fill it on the website (use AI)


now, clangd is in charge of intellisense
because it has read the commands used to compile each source file.

these commands that compile each source file
are executed with CMake: Build


>> This is actually DANGEROUS! The above line should read: "... are WHAT CMAKE EXECUTED"
>> Intellisense may become out of sync with what your CMake (i.e. compiler) flags are, 
    if you change your flags without manually updating compile_commands.json (in the case that
    CMake is not generating this file, like with me)
>> compile_commands.json SHOULD BE GENERATED BY CMAKE, and then READ BY clangd TO 
    AUTOMATICALLY INFORM INTELLISENSE


------- FIX 2 ------ (PROPER)

Setting Cmake build generator to NINJA in settings.json fixed this problem.
Now cmake will AUTOMATICALLY update compile_commands.json, for clangd to read and operate intellisense with.


-------------

... why did I never need to tell it where the linker was

You have selected the KIT for cmake when you uopen the project (x64 MSVC)
from build-ninja/CMakeCache.txt, you can see CMAKE_LINKER as link.exe in same
directory as cl.exe from MSVC.

--------
