{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug PET (CodeLLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/PET",
      "args": ["10", "10"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: build"
    }
  ]
}
